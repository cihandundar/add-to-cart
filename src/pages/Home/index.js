import { ShoppingItem } from "components";
import React from "react";

const Home = () => {
  return (
    <main className="main">
      <section className="section">
        <ShoppingItem />
      </section>
    </main>
  );
};

export default Home;
